---
title: Installation
---

To run the app as a binary (express server app), you can simply type the command:

```shell
npx wp-graphql-gutenberg-server
```

The app supports `--host` and `--port` arguments with defaults set to `0.0.0.0` and `5000`.
